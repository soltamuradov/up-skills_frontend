{
  "name": "root",
  "private": true,
  "scripts": {
    "packages-deps": "cd packages/addquestions && npm install && cd ../questionslist && npm install && cd ../host && npm install"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "external-remotes-plugin": "^1.0.0",
    "lerna": "^7.4.2"
  }
}
